[project]
name = "viewer-camera"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.100",
    "uvicorn>=0.20",
    "platform-utils",
    "httpx>=0.28.1",
]

[tool.uv.sources]
platform-utils = { path = "../../utils", editable = true }
