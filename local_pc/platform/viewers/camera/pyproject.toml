[project]
name = "viewer-camera"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.100",
    "uvicorn>=0.20",
    "platform-schemas",
]

[tool.uv.sources]
platform-schemas = { path = "../../schemas", editable = true }
